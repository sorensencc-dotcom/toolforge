# Toolforge Telemetry API — environment template.
# Copy to .env (git-ignored) and adjust as needed. DB_PATH is NOT configured
# here — it is derived from __dirname in server.js so no absolute path is
# ever committed (see design doc C12).

PORT=3000
