---
name: ci-cd-fix-cascade-failures-2026-07-03
description: CI/CD outage investigation - v3 deprecation fix + cascading failures after attempted Node.js 20 pin
metadata: 
  node_type: memory
  type: project
  originSessionId: 8ed91774-4707-4f64-8180-e6601008ef76
---

## Timeline

**2026-07-02:** Fixed 9-day CI outage (v3 → v4 artifact actions)
- Root cause: GitHub deprecated actions/upload-artifact@v3 (April 2024), enforcement tightened June 2024
- Fix: Replaced v3 with v4 across 9 repos
- Pushed to: claude-skills, castironcharlie, charlie-deep-research, rewritelabs.io, CIC-DAG, CIC, cic-ingestion, CIC_MEDIA_LIBRARY, fds.fx.reporting
- Status: ✅ FIXED (CI validators passed)

**2026-07-02 (continued):** Addressed Node.js 20 deprecation warning
- GitHub deprecated Node.js 20 on runners, forced actions onto Node.js 24
- Attempted fixes (in order):
  1. Upgraded to latest patch versions (v4.1.7, v4.3.6) — didn't work
  2. Added setup-node@v4 with node-version: '24' — didn't work
  3. Removed patch pinning to use @v4 (latest) — didn't work
  4. Pinned runners to ubuntu-20.04 — didn't work
  5. Force set node-version: '20' in setup-node — UNKNOWN (awaiting results)

**2026-07-03:** Cascading failures reported
- 48-hour failure window (since ~2026-07-01) affects all 10 repos
- Nightly Validators failing in 1-3 seconds (early failure, not timeout)
- rewrite-docs CI failing in 9-10 seconds (npm install likely failing early)
- cic-ingestion phase-1-gate.yml showing "No jobs were run" (YAML schema error?)
- Pattern suggests shared root cause (not code bugs)

## Suspected Root Causes

1. **Expired GitHub secret/token** (most likely)
   - 48-hour failures align with possible secret rotation on 2026-07-01
   - Affects all repos equally

2. **Our workflow edits** (possible)
   - force-node-20.ps1 modified YAML via regex replacement
   - Could have introduced indentation errors or malformed steps
   - But syntax checks show workflows look correct locally

3. **Runner image change by GitHub** (possible)
   - New ubuntu-20.04 image might be missing dependencies
   - Or ubuntu-20.04 deprecated by GitHub

## Verified

- ✅ YAML syntax looks correct (no obvious indentation issues)
- ✅ No duplicate setup-node steps
- ✅ Proper indentation: setup-node block aligns with checkout
- ✅ v3→v4 upgrade itself was correct

## Next Steps

1. **Get actual error logs** from GitHub Actions (one failing run per symptom)
   - rewrite-docs nightly-validate error message
   - cic-ingestion phase-1-gate.yml "No jobs were run" exact error
   - Stack trace or build log if available

2. **Check shared resources**
   - GitHub secrets: did any expire on 2026-07-01?
   - Environment variables (GITHUB_TOKEN, API keys)?
   - Upstream API dependencies?

3. **Consider reverting**
   - If error logs point to our workflow edits, revert force-node-20 changes
   - Keep v3→v4 fixes (those were necessary and working)
   - Revert: ubuntu-20.04 pin, explicit node-version: '20' pin

## Files Modified

All 10 repos received workflow edits:
- v3 → v4 (artifact actions) — KEEP
- Patch version pinning → removed — MAYBE REVERT
- ubuntu-20.04 pin added — MAYBE REVERT
- setup-node + node-version: '20' — MAYBE REVERT

Scripts created:
- fix-artifact-deprecation.ps1 (working)
- fix-node-24-compat.ps1 (didn't work)
- fix-node-24-setup.ps1 (didn't work)
- pin-ubuntu-20.ps1 (uncertain)
- force-node-20.ps1 (uncertain — this is likely the culprit)
- revert-last-edits.ps1 (ready if needed)
