# T07 Open Notebook consumer integration

Phase B handoff. Phase A does not contain a consumer integration test.

Expected checkout: `toolforge/integration-harness/toolforge-herdr-trm-integration`.

Before implementation, Phase B must provide:

- a stable internal `runtime-owner` API or an approved extracted package;
- a pinned Open Notebook fixture revision and reproducible Ubuntu runner;
- exact command arguments, working directory, port allocation, health-wait, and environment allowlist;
- TRM consumer assertions for lifecycle events; and
- signed approval from Toolforge, TRM, and Open Notebook owners.

Placeholder commands, unpinned dependency versions, and example credentials are not executable CI configuration.
