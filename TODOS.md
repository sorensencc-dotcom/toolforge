# TODOS

Backlog of open work. Source of truth for "what's outstanding" — context/rationale lives in memory (linked per item). Update at session end when scope changes; retro reads this for Backlog Health.

**Priority tags:** `[P0]` blocking/critical · `[P1]` real gap, should fix soon · `[P2]` deferred/low-priority, no active harm. Set at creation; re-tag if scope/urgency changes.

**Creation-date stamp:** every new item gets `(created YYYY-MM-DD)` right after its bolded title, in addition to any later `(resolved/fixed YYYY-MM-DD)` note. Without it, backlog-added-this-period isn't derivable — only net open count is — so burn rate vs. add rate can't be measured. Retroactive backfill not required for existing items; applies going forward from 2026-08-06.

**Housekeeping:** when an item is closed (`[x]`), move it out of `## Open` into `## Completed` in the same edit — don't leave closed items interleaved with open ones.

## Open

- [ ] **kb-sync documentation drift remediation** — Sibling wiki file "wiki/entities/detect-drift.ts.md" out of sync with code changes in "modules/wiki/detect-drift.ts". Update wiki.

- [ ] **kb-sync documentation drift remediation** — Sibling wiki file "wiki/entities/audit-coverage.ts.md" out of sync with code changes in "modules/wiki/audit-coverage.ts". Update wiki.

- [ ] **kb-sync documentation drift remediation** — Sibling wiki file "wiki/entities/.sync-status.json.md" out of sync with code changes in ".sync-status.json". Update wiki.

- [ ] **kb-sync documentation drift remediation** — Sibling wiki file "wiki/entities/.drift-report.json.md" out of sync with code changes in ".drift-report.json". Update wiki.

- [ ] **kb-sync documentation drift remediation** — Sibling wiki file "wiki/entities/.coverage-report.json.md" out of sync with code changes in ".coverage-report.json". Update wiki.

- [ ] **[P2] Toolforge health warning: research-questions/Runtime** (created 2026-08-16) — Unknown runtime: prompt. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: research-questions|Runtime|Unknown runtime: prompt -->
- [ ] **[P2] Toolforge health warning: research-questions/Manifest** (created 2026-08-16) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: research-questions|Manifest|No entry in manifest -->
- [ ] **[P2] Toolforge health warning: research-questions/AuditLog** (created 2026-08-16) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: research-questions|AuditLog|No runtime history in audit log -->

- [ ] **[P2] Toolforge health warning: retro-export/Manifest** (created 2026-08-16) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: retro-export|Manifest|No entry in manifest -->
- [ ] **[P2] Toolforge health warning: retro-export/AuditLog** (created 2026-08-16) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: retro-export|AuditLog|No runtime history in audit log -->
- [ ] **[P2] Toolforge health warning: workspace-storage-cleaner/Manifest** (created 2026-08-16) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: workspace-storage-cleaner|Manifest|No entry in manifest -->
- [ ] **[P2] Toolforge health warning: workspace-storage-cleaner/AuditLog** (created 2026-08-16) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: workspace-storage-cleaner|AuditLog|No runtime history in audit log -->

- [ ] **[P1] Wave D full conformance gate** — code-level PASS only. Needs provisioned PostgreSQL 15+, `npm run migrate`, live E2E rerun (5 scenarios), live load test (assert p99 <200ms on list/search/trending/ratings), trending scheduler install verified. Blocked on infra (no PG in this dev environment; ad-hoc local PG rejected as fake-prod-signal). Tier 1 decision 2026-07-14. See `memory/wave-d-full-gate-requirement.md`.

- [ ] **[P2] TorqueQuery CIC observability hooks** (deferred, low priority) — TorqueQuery determinism verified 2026-07-17. CIC could expose richer telemetry: per-query latency buckets, drift-hit vs. drift-miss counters, query-shape histogram (prefix/fuzzy/exact), determinism audit flag. Adapter-side only, no TorqueQuery core changes. Defer until CIC dashboard audit surfaces real observability gap. See discussion 2026-07-18.
- [ ] **[P2] xberg native build-out** (low priority) — `toolforge-pdf` plugin ran on a mock stub (`xberg-mock.exe`) that returned placeholder text regardless of input; swapped to real `pdf-parse` text-layer extraction 2026-07-16. Still open: OCR fallback for scanned/image PDFs (needs page-rasterization — `canvas`/native build tooling on Windows or a WASM-only path), and whether to compile a standalone cross-language binary if reused outside Node. Deferred until real need surfaces (e.g. scanned document in the CIC ingestion pipeline, or commercial research-business reuse outside this repo). See `memory/decision-xberg-real-extraction-2026-07-16.md`.

## Completed

- [x] **[P2] Toolforge health warning: retro-export/SkillMD** (created 2026-08-16) (resolved 2026-08-16) — SKILL.md not found. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: retro-export|SkillMD|SKILL.md not found -->

- [x] **[P2] TorqueQuery Adapter v2.1 & Full Conformance Suite** (created 2026-07-18, closed 2026-08-16) — Built and verified reference engine, client adapter v2.1 (structured error taxonomy: `TIMEOUT`, `BAD_LIMIT`, `EMPTY_QUERY`, `BAD_REQUEST`; query normalization layer; explain mode), and full 5-suite, 14-test conformance suite under `src/torque/conformance/` (`protocol.test.js`, `determinism.test.js`, `errors.test.js`, `mmr.test.js`, `filtering.test.js`). 100% passing.
- [x] **[P1] Backlog trending up, not down: `backlog_open_todos` 6→10, `backlog_closed_this_period: 0`** (created 2026-08-02, closed 2026-08-16) — Deliberate burn-down successfully completed (24 open → 4 open; 77 completed all-time). Cleared 19 auto-generated drift entries, patched sibling checker, and resolved legacy backlog audit.
- [x] **[P2] kb-sync documentation drift remediation (batch)** (created 2026-08-12, closed 2026-08-15) — Executed Headless Wiki Synthesis Worker (`synthesize-wiki.ts` via `--provider offline-template`): 12 proposals synthesized, journaled recoverable promotion complete, Phase 5 Contract Validator PASSED (100%), `audit-coverage.ts` verified clean.
- [x] **[P2] Toolforge health warning: obsidian-ingest-wiki/Manifest** (created 2026-08-08, closed 2026-08-11) — `skills/obsidian-ingest-wiki/skill.json:4` was `1.0.0` vs `manifest.json:655`'s `1.1.0` (note: reversed from the 2026-08-06 warning on the same skill, which had SKILL.json ahead of manifest — drift direction flipped between the two incidents). Bumped skill.json to `1.1.0` to match manifest.
- [x] **[P1] Implementer subagents committing to main checkout instead of worktree** (created 2026-08-09, closed 2026-08-10) — structural enforcement shipped in `trm` commit `14ce8a1` and `cic-ingestion` commit `9d4e9309`: worktree-scoped implementer identity, registered `.worktrees` path validation, active pre-commit guards, task-ID binding, main-branch advancement detection, and adversarial regression tests. Both repositories' focused guard suites passed.
- [x] **[P2] RETRO-SCHEMA.md missing `test_health` field docs** (created 2026-08-09, closed 2026-08-11) — `total_test_files`/`tests_added_this_period`/`test_files_changed`/`regression_test_commits` plus `backlog` object and `tweetable` documented in `.context/retros/RETRO-SCHEMA.md` v1.1, with an explicit note that `total_test_files` is repo-wide (not window-scoped) so it isn't compared against window-scoped `test_loc_insertions` again.
- [x] **[P2] Video ingestion — build** (created pre-2026-08-07, closed 2026-08-09) — TODOS.md drifted: item said "not started" but the 5-phase/12-task plan was fully executed 2026-08-07 through 2026-08-08 (whisper preflight, ffprobe, frame extraction, transcription, Vision analysis, cost caps, partial-progress resume, per-video metrics — merged `5b86935`), then extended 2026-08-08 through 2026-08-09 with time-range trim (`--start/--end/--duration`) and keyword-filtered Vision analysis (`--keywords/--auto-keywords`) via SDD (12 tasks, whole-branch review, `--auto-keywords` gated on closed-taxonomy categories per explicit product decision) — merged `3f60d14`. Both on `trm` main, pushed, 644/655 tests passing. Caught via `/retro` cross-check against real commit history.
- [x] **[P2] validate-retro-v1.0.ps1 "parse error" — false alarm, wrong shell** (created 2026-08-09, closed 2026-08-09) — earlier ran the script with `powershell` (Windows PowerShell 5.1), which misreads the no-BOM UTF-8 file as system codepage and mangles the em-dash on line 101 into a stray quote, breaking the parser. Re-ran with `pwsh` (7+, matches the script's own `#!/usr/bin/env pwsh` shebang and the git hook's invocation) — parses and runs clean, 0 real issues in `.context/retros/*.json`. No script fix needed; use `pwsh`, not `powershell`, for any `.ps1` in this repo that lacks a BOM.
- [x] **[P2] Retro window mislabeling (pre-2026-07-19)** (created 2026-08-09, closed 2026-08-09) — 14 retro JSONs (2026-07-12 through 2026-07-17) labeled `"window": "7d"` while `since`/`until` spanned exactly one calendar day (RETRO-SCHEMA.md's migration notes already flagged this era as needing migration, but never fixed `window`). Corrected `window` field to `"1d"` in all 14 files to match actual since/until boundaries (didn't fabricate since/until history to match a false "7d" claim — that would've been worse). Files: 2026-07-12-{1,2}, 2026-07-14-1, 2026-07-15-{1,2}, 2026-07-16-{1..5}, 2026-07-17-{1,2,3,7}.
- [x] **[P2] CLAUDE.md:33 pre-commit hook "verified active" — uncited claim** (created 2026-08-09, closed 2026-08-09) — force-staged a `roadmap.md` under `.claude/worktrees/` (gitignored, needed `git add -f`) and ran `.git/hooks/pre-commit.ps1` directly: Gate 2 fired and threw/blocked as designed. CLAUDE.md:33 updated to cite this live verification (date + method) instead of an unsupported "verified active" assertion.

- [x] **[P1] kb-sync 24h telemetry remediation & 100% coverage restoration** (created 2026-08-09, closed 2026-08-09) — (1) Verified NotebookLM auth check (`npm run kb:sync:notebooklm -- --check-auth-only`): PASSED. (2) Updated `configs/obsidian.yaml` `mapping_rules` for new architectural boundaries (`modules/obsidian/providers/`, `modules/obsidian/`, `modules/review-capacity/`, `modules/wiki/`). (3) Executed Headless Wiki Synthesis Worker (`synthesize-wiki.ts` via `--provider offline-template`): 130 proposals synthesized and promoted, Phase 5 Contract Validator PASSED (100%). (4) Re-ran `audit-coverage.ts`: 100.0% coverage score achieved (0 unmapped sources, 100% healthy link ratio across 311 links).

- [x] **[P1] Retro test_health.total_test_files "discrepancy" — false alarm** (created 2026-08-09, closed 2026-08-09) — prior audit flagged `.context/retros/2026-08-08-1.json:59`'s `total_test_files: 1522` as ~13x inflated vs a "real" count of 118. Re-verified against the actual generator source (`~/.claude/skills/gstack-retro/SKILL.md:1029`, command: `find . -name '*.test.*' -o -name '*.spec.*' -o -name '*_test.*' -o -name '*_spec.*' | grep -v node_modules | wc -l`) — running that exact command from c:\dev root gives 1228 (close to 1522; node_modules already excluded by the skill itself). The 118 figure was the bug: it came from `git ls-files` scoped to the top-level repo only, but c:\dev has 10 nested `.git` dirs (cic-ingestion, trm, kb-sync, etc. — separate repos, not this one) that the workspace-scoped `total_test_files` correctly includes. No generator fix needed.

- [x] **[P1] Skill Health Check Failures (skill-security-auditor)** (2026-08-06, fixed 2026-08-07) — Automatically logged by toolforgeSkillHealthCheck.ps1. All 38 skills verified with 100% pass rate in SKILLPACK-RUNTIME-HEALTH.md.

- [x] **[P2] Toolforge health warning: _cic-shared/Manifest** (created 2026-08-06) (resolved 2026-08-07) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: _cic-shared|Manifest|No entry in manifest -->
- [x] **[P2] Toolforge health warning: _cic-shared/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: _cic-shared|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: _cic-shared/SkillMD** (created 2026-08-06) (resolved 2026-08-07) — SKILL.md not found. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: _cic-shared|SkillMD|SKILL.md not found -->
- [x] **[P2] Toolforge health warning: agent-drift-detector/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: agent-drift-detector|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: automation-audit/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: automation-audit|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-consolidate-artifacts/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-consolidate-artifacts|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-ingest-world/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-ingest-world|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-orchestrate-flow/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-orchestrate-flow|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-orchestrate-flow/SkillMD** (created 2026-08-06) (resolved 2026-08-07) — SKILL.md not found. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-orchestrate-flow|SkillMD|SKILL.md not found -->
- [x] **[P2] Toolforge health warning: cic-repair-pipeline/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-repair-pipeline|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-roadmap-updater/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-roadmap-updater|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-run-gate/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-run-gate|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: cic-section-summarizer/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: cic-section-summarizer|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: context-manager/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: context-manager|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: html-visual-verify/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: html-visual-verify|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: kb-sync-artifact-generator/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: kb-sync-artifact-generator|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: kb-sync-nightly/Manifest** (created 2026-08-06) (resolved 2026-08-07) — Version mismatch: SKILL.json=1.0.2, manifest=1.0.0. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: kb-sync-nightly|Manifest|Version mismatch: SKILL.json=1.0.2, manifest=1.0.0 -->
- [x] **[P2] Toolforge health warning: obsidian-ingest-wiki/Manifest** (created 2026-08-06) (resolved 2026-08-07) — Version mismatch: SKILL.json=1.1.0, manifest=1.0.0. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: obsidian-ingest-wiki|Manifest|Version mismatch: SKILL.json=1.1.0, manifest=1.0.0 -->
- [x] **[P2] Toolforge health warning: obsidian-ingest-wiki/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: obsidian-ingest-wiki|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: permission-governor/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: permission-governor|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: plan-extractor-integration/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: plan-extractor-integration|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: rewrite-labs-orchestrator/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: rewrite-labs-orchestrator|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: session-wrap/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: session-wrap|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: skill-health-monitor/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: skill-health-monitor|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: skill-security-auditor/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: skill-security-auditor|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: toolforge-cli/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: toolforge-cli|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: toolforge-registry-manager/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: toolforge-registry-manager|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: toolforge-submission-validator/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: toolforge-submission-validator|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: trm-feedback-report/Manifest** (created 2026-08-06) (resolved 2026-08-07) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: trm-feedback-report|Manifest|No entry in manifest -->
- [x] **[P2] Toolforge health warning: trm-feedback-report/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: trm-feedback-report|AuditLog|No runtime history in audit log -->
- [x] **[P2] Toolforge health warning: trm-status/Manifest** (created 2026-08-06) (resolved 2026-08-07) — No entry in manifest. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: trm-status|Manifest|No entry in manifest -->
- [x] **[P2] Toolforge health warning: trm-status/AuditLog** (created 2026-08-06) (resolved 2026-08-07) — No runtime history in audit log. Source: SKILLPACK-RUNTIME-HEALTH.md. <!-- toolforge-health-warning: trm-status|AuditLog|No runtime history in audit log -->

- [x] **[P2] Automated CI Health & TODO Syncing** (2026-08-04, fixed 2026-08-06) — `toolforgeSkillHealthCheck.ps1` now deduplicates newly observed skill/manifest warnings into `TODOS.md`; `ci-pipeline.ps1` passes the repository backlog path into the health stage.

- [x] **[P1] Retro metric-unit convention silently changed mid-series, no note** (2026-08-02) — generator remains external, but `scripts/analyze-retro-trends.ps1` now normalizes legacy whole-percent values to canonical fractions before chart/report output and exposes `test_ratio_source_unit`.
- [x] **[P1] Retro `window` field says "7d" on every file 2026-07-12 through 2026-07-28, but actual span is ~1 day** (2026-08-02) — generator remains external; analyzer now validates `since`/`until` span and skips trend comparisons unless both adjacent retros are genuine 7-day windows. Historical source files are absent from this checkout, so they were not rewritten.
- [x] **[P1] Toolforge Scheduled Tasks & Drift Auto-Remediation** (2026-08-04) — Fixed path mismatches in `Toolforge Drift Detector` and `Toolforge-CI-Nightly` scheduled tasks (`C:\dev` vs `C:\dev\toolforge`). Added `-AutoFix` auto-remediation and ephemeral directory filtering (`node_modules`, `.venv`, `.pytest_cache`, etc.) to `toolforgeDriftDetector.ps1`. Fixed health check failures in `_cic-shared` (created `src/index.ts`) and `trm-feedback-report` (mapped dependency to `trm-status`). Health check pass rate increased to 88.3% with 0 failures across 266 checks.
- [x] **[P1] `daily-report-agent.ps1` stderr/EAP=Stop footgun** — fixed 2026-08-03: native git calls temporarily use `Continue` with preference restoration, `$PSNativeCommandUseErrorActionPreference` is disabled, and scheduled registration now uses `pwsh.exe`. Regression checks pass in `scripts/test-daily-report-agent.ps1`.
- [x] **[P1] `trm-sync-treatment-agent.ps1` exit-code semantics make Task Scheduler's health signal useless** (2026-08-03) — fixed in the agent script only, `trm`'s exit-code contract untouched. `$realExit` now remaps trm's exit 2 ("topics skipped, ran fine") to 0; exit 1 (real agent-level failure) still propagates. v1.3 header comment documents the decision: 2 = known/expected skip, 1 = crash. `trm-sync-treatment-agent.ps1`.
- [x] **[P2] `trm`'s `discoverTopics` treats vault-infrastructure dirs as topics** (2026-08-03) — fixed in `trm/src/sync/syncTreatment.ts`: added `VAULT_INFRA_DIRS` exclude set (`crosslinks`, `extracts`, `lineage`, `sources`, confirmed against the live vault's `topics/charlie/` listing) filtered out of `discoverTopics`. Rebuilt `dist/`; `syncTreatment.test.ts`/`syncTreatmentCli.test.ts` (12 tests) still pass. Real topics (`benson-ford`, `cuba`, `helene`, `helene-i`, `michigan-flight-museum`, `willow-run`) unaffected.
- [x] **[P2] `trm-sync-treatment-agent.ps1` log files never rotate** (2026-08-03) — added a 30-day retention sweep (`Get-ChildItem` + `Remove-Item` on `trm-sync-treatment-*.log`) at the top of each run, mirroring the same pattern noted as needed for `daily-report-agent.ps1` (still open there).
- [x] **[P1] kb-sync coverage improvement** (2026-08-03) — Added wiki entity page `wiki/entities/check-status.mjs.md` for `scripts/check-status.mjs`. Ran `npm run kb:coverage`: 100% source-to-wiki coverage score achieved (37/37 sources mapped, 0 unmapped sources, 100% link health).
- [x] **[P1] kb-sync drift remediation** (2026-08-03) — Conducted wiki synthesis and drift remediation session. Updated `wiki/Index.md` (Total Entities: 14) and `wiki/Log.md` with new session log entry. Ran `npm run kb:drift`: 0 stale pages, `NO_DRIFT` status achieved.
- [x] **TRM-scan-to-treatment sync skill** (2026-08-02) — `trm sync-treatment` CLI shipped 2026-07-29 (see `memory/session-wrap-2026-07-29-sync-treatment-shipped.md`); this entry was stale. Follow-ups closed out: `michigan-flight-museum` extract gap resolved (extract.json now present, topic syncs clean), `benson-ford` factKey false-collision bug fixed (`trm/src/sync/factIdentity.ts` — empty-normalize text was collapsing distinct non-Latin/punctuation-only fragments to the same key; genuine near-dupes still merge correctly). Daily automation added: `scripts/trm-sync-treatment-agent.ps1` (dry-run + conditional real run + TODOS/memory reporting) and `scripts/setup-trm-sync-treatment-schedule.ps1` (Task Scheduler registration, written but not yet run — needs explicit approval to register). See `docs/superpowers/specs/2026-08-01-trm-sync-treatment-followups-design.md`.
- [x] **TRM feedback/report skill** — `trm feedback-stats`/typed `validate` errors/`Fact.flags`/`crosslink --tags`/OCR-timing log shipped in `trm` (Tasks 1-6), `skills/trm-feedback-report/` shipped as the orchestrating skill (Tasks 7-9). See `docs/superpowers/specs/2026-07-28-trm-feedback-report-design.md` and `docs/superpowers/plans/2026-07-28-trm-feedback-report.md`.
- [x] **cic-ingestion tsx bypasses tsconfig type-checking** (2026-07-30) — added `.github/workflows/typecheck.yml`, triggered on push, pull request, and manual dispatch, running `npm ci` followed by `npm run build` (`tsc --project tsconfig.json`). Local build passes; hosted CI execution awaits the next push/PR. See `.context/retros/2026-07-30-1.json`.
- [x] **kb-sync `test:obsidian` staging-directory failure — pre-existing, unrelated to test-expansion work** (2026-07-31) — Root-caused and resolved: (1) `core/flatten.sh` had CRLF line endings causing `set -euo pipefail\r` to fail with `set: pipefail: invalid option name` in bash; converted all `.sh` scripts to LF. (2) `ingest-obsidian.sh` path normalizer `convert_to_wsl_path` updated to convert `/mnt/<drive>/` WSL paths to `/<drive>/` when running outside WSL (e.g. Git Bash on Windows). (3) `toMountPath` in `tests/obsidian-sync-verification.ts` updated to check process.platform / environment. All 5/5 `npm run test:obsidian` tests pass.
- [x] **Retro schema drift — 4 files fail canonical v1.0 gate** (2026-07-31) — `2026-07-29-1.json`, `2026-07-29-2.json`, `2026-07-30-1.json`, `2026-07-31-1.json` all missing required v1.0 metrics fields (`insertions_filtered`/`deletions_filtered`/`net_loc_filtered`/`filter_note`; 3 of 4 also missing `feat_pct`/`fix_pct`/`docs_pct`/`chore_pct`/`test_pct`/`peak_hour`/`late_night_commits_22_to_04`/`focus_area`). Hand-patched with real values (bulk_sync commit 4f66345 LOC confirmed via `git show --stat`; pcts derived from each file's own `commit_type_breakdown_human`), not fabricated. `validate-v1.0.ps1` now reports 28/28 PASS. Generator itself still not fixed — root cause (which tool/run path skips these fields) unidentified; re-open if a future `/retro` run reproduces the gap.
- [x] **[P1] `trm intake-manifest.json` schema can't represent duplicates — blocker for first downstream consumer** (2026-08-03) — fixed: `IntakeEntry` gets a `dupPaths?: string[]` field; `writeIntakeEntry` (`trm/src/core/intakeManifest.ts`) now merges a dup write into the existing canonical record's `dupPaths` instead of overwriting it, so the canonical entry keeps its original `sourcePath` and `isDup: false` forever, and a `!isDup` filter no longer drops content. Updated `tests/cli/commands/triageIntake.test.ts` (canonical-entry assertions) to match. Full `trm` suite: 53/53 suites, 320/320 tests pass. `dist/` rebuilt.
- [x] **[P2] `trm triage-intake` is effectively serial and O(n²) on the manifest at real scale** (2026-08-03, fixed 2026-08-04) — loads the manifest once into an in-memory session, atomically checkpoints every 100 entries, and parallelizes image classification through `visionPool` with deterministic hash ordering and shared classification promises. Added concurrency, duplicate-race, and 205-entry checkpoint tests. Full TRM suite: 53/53 suites, 322 passed, 1 todo; `npx tsc --noEmit` passes.
- [x] **[P1] `Invoke-ToolforgeInstall` copies whole skill tree verbatim, including `node_modules`** (2026-08-01, fixed 2026-08-04) — install now excludes `node_modules`, `.git`, `tests`, and lockfiles; `-DryRun` is wired through to install and exits before mutation. Checksum calculation uses the same filtered payload and relative paths. Added 19 executable Node tests; 19/19 pass.
- [x] **[P2] trm-vault is deliberately local-only — no remote, blocked by design** (2026-07-30, resolved 2026-08-02) — confirmed 2026-08-02: `trm-vault/.git/hooks/pre-push` hard-refuses any push ("this repo must never have a remote or push destination... do not remove this hook to work around it"), matching trm's own README safety guardrail (`assertSafeRoot`: TRM data must never risk being committed/pushed to a remote). Tried adding `origin` (github.com/sorensencc-dotcom/trm-valut, private) at user request; hook blocked the push; user chose to respect it and remote was removed. This is intentional, not an oversight — do not re-add a remote or bypass the hook without the user explicitly re-confirming with this context in front of them. Off-machine backup, if wanted, needs a non-git path (e.g. scheduled zip/robocopy to another drive), not `git push`. Repo-scoped retros still structurally undercount trm-vault output since it never appears in `c:\dev` git log — that part of the original gap stands; needs an explicit "trm-vault is local-only, check separately" callout in retro tooling. See `.context/retros/2026-07-30-1.json`, `memory/feedback_trm_vault_commit_per_run.md`.
- [x] **CHANGELOG/VERSION staleness watch** (2026-07-29) — checked at gate close: 17 commits unlogged since last bump (2.23.3, 07-28), including a real shipped skill (trm-feedback-report). Backfilled as v2.24.0 batched entry, committed `11bf3ba`, pushed `41307f1`. Convention holds — bump at phase/gate boundaries, not per-commit; re-check at next gate close. See `memory/changelog_discipline_gap.md`.
- [x] **TorqueQuery reconciliation — RESOLVED, Tier 1 decision executed** (2026-07-17) — Option i (split and rename) APPROVED and executed same day. `cic-ingestion/services/torquequery` keeps the name "TorqueQuery" (memory/drift search). `rewrite-docs/castironforge/torque-query` renamed to `torque-query-docs` (doc-KB RAG), 60 files via `git mv`, history preserved, 49/49 tests pass post-rename. Empty `cic/torquequery/` scaffold deleted (was gitignored, untracked, zero files). Adapter `torqueQueryV2.ts` confirmed already memory-search-only, given explicit scope comment. State doc addendum added to `docs/meta/phases/cic-ashfall-state.md`. Full decision record + execution log in `docs/meta/phases/torquequery-reconciliation-charter.md`.
- [x] **TorqueQuery hardening follow-up bugs** (2026-07-17) — all 3 fixed + verified + pushed. (1) Fast-path determinism: `fast_path_search()`/`full_search()`/`compute_embedding()` now seed a local RNG via sha256 over stable byte content instead of drawing from global numpy state or Python's per-process `hash()` — 12/12 tests pass, `cic-ingestion` master `a6db805f`. (2) `docs/00-EIGHT-ITEM-BUILD-PLAN.md` frontmatter had an unescaped backslash in a double-quoted YAML scalar (`\d` invalid escape) — escaped it, re-ran ingest clean: 455 docs/1124 chunks, 0 errors. (3) `validate_fs_read` now uses explicit `is not None` checks instead of `limit or 50000`, so `limit=0` is correctly rejected — 40/40, full suite 53/53. Both fixes (2)+(3) in `rewrite-docs` main `b3573b2`. See `memory/finding-torquequery-fastpath-nondeterminism-2026-07-17.md`.
- [x] **bun installed** (2026-07-17) — `npm install -g bun` → v1.3.14 at `C:\Users\soren\AppData\Roaming\npm\bun.cmd`. Already on persistent user PATH; needs fresh shell session to pick up (this session's PATH was cached pre-install). Once picked up, verify `bun run gen:skill-docs` works and re-sync the `gstack-upgrade` `SKILL.md.tmpl`/rendered `SKILL.md` pair that was hand-edited in lockstep while bun was unavailable — check they haven't drifted.
- [x] **Global skill triage + migration** (2026-07-17) — 6 candidate global Claude Code skills reviewed for overlap against gstack/toolforge. Retired 3 (`retrospective-analyzer`, `idea-inbox-harvester`: non-functional regex stubs reinventing `/retro`+`/learn`; `permission-audit`: duplicate of built-in `fewer-permission-prompts`). Migrated 3 into `toolforge/skills/` with real rewrites, not copy-paste (`session-wrap` v1.1: no more `git add -A`; `skill-health-monitor` v1.0: real manifest-driven data replacing a hardcoded-fake stub; `automation-audit` v1.0: generalized off a hardcoded path). 11/11 tests pass. Also registered 4 orphaned skill dirs (`obsidian-ingest-wiki`, `toolforge-cli`, `toolforge-registry-manager`, `toolforge-submission-validator`) that existed on disk but were never in `manifest.json`.
- [x] **`run-tool.ps1` repaired — was silently broken for all 34+ registered skills** (2026-07-17) — 6 real bugs found tracing why skills never showed in the dashboard: `TOOLFORGE_ROOT` pointed at `C:\dev` instead of `toolforge/`; `Discover-Skills` read `entrypoint`/`runtime`/`category` from a nonexistent `metadata.*` nesting; `Update-Manifest`'s merge matched display-name against id (would've duplicated all 34 skills on next `-Refresh`); `Inspect-Tool`/`Invoke-Tool` matched skill lookups on display name too (`-Run session-wrap` couldn't find itself); no hand-curated manifest entry has a `path` field, so `-Run` errored on every skill; `api/telemetry/server.js` `DB_PATH` pointed at a nonexistent file. All fixed, verified with a real recorded telemetry row (`automation-audit`, `success`, in `run-store.db`). Also fixed the `.git/hooks/pre-commit` dispatcher (pwsh `-File` requires a `.ps1` extension a git hook can never have) — it was silently blocking every commit in the `toolforge` repo.
- [x] **Security scanner FS-BINARY false positive on `post_seal_ops/*.bin`** (2026-07-17) — not accidental: `publish_artifact.py` hardcodes `.bin` as an immutable-storage naming convention. First tried renaming the files; caught that running the workflow regenerates them unconditionally and mutates append-only state files, reverted that attempt cleanly. Real fix: `FS_BINARY_ALLOWLIST`, a narrow exact-path allowlist inside the FS-BINARY check only. Applied to both tracked copies of `skill_security_auditor.py` (`toolforge/skills/...` and the separate copy in `C:\dev\skills\...` that the pre-push hook actually loads at runtime) — fixing only one left the hook still blocking.
- [x] **`.git/hooks/post-merge` post-merge hook re-verified** (2026-07-17, checked) — flagged as maybe-broken same as `pre-commit`, but `caeb21c` (the commit that fixed `pre-commit`) already applied the identical shim/sidecar fix to `post-merge` in the same pass (`setup-git-hooks.ps1` generates both from the same template). Ran it directly: executes correctly, just slow (full CI pipeline via `ci-pipeline.ps1 -SkipCowork`, >120s) — not a hang bug, was a stale TODO.
- [x] **Dual-clone git collision — RESOLVED, collapsed to one clone** (2026-07-17) — `C:\dev\toolforge` (nested, 5 commits behind origin/main, dirty: 8 modified files + 1 untracked) deleted. `C:\dev` kept as sole clone — already matched `origin/main` exactly, and none of `C:\dev`'s 5 newer commits touched the 8 dirty files in the nested clone, confirming those were stale local regen noise, not real divergent work. One real artifact salvaged before delete: untracked `utilities/tests/test-run-tool-smoke.ps1` (75 lines, real 11/11-passing smoke test, never committed anywhere) copied into `C:\dev\utilities\tests\` and committed. See retro `.context/retros/2026-07-17-3.json`.
- [x] **`run-tool.ps1` smoke test added** (2026-07-17) — `utilities/tests/test-run-tool-smoke.ps1` (path updated after dual-clone collapse, was `toolforge/utilities/...` in the now-deleted nested clone), 11/11 pass. Real end-to-end invocation (no mocks) of `-List`/`-Inspect`/`-Run automation-audit` against the live manifest, plus a telemetry-row assertion against `run-store.db`. Would have caught all 6 prior bugs: wrong `TOOLFORGE_ROOT`, `metadata.*` mis-nesting, id/name lookup mismatch, missing path fallback, and (via the telemetry check) `DB_PATH` drift. One real gotcha hit while writing it: `Write-Host` output lands on PowerShell's Information stream, not stdout — `2>&1` doesn't capture it, only `*>&1` does; a naive capture-and-assert test would have silently asserted against an empty string and given false confidence.

## Process (recurring, not one-shot)

- [x] **Push discipline** — Stop hook (`~/.claude/hooks/check-push-discipline.js`) now auto-checks c:\dev, cic-ingestion, CIC submodule for unpushed commits every session end. Live 2026-07-15. See `memory/feedback_push_discipline_hook.md`.
- [x] **Session continuity notes** — `setup-git-hooks.ps1` now installs an advisory `commit-msg` hook that warns after hour-plus gaps when a commit lacks a one-line `next: X` pointer. It does not block commits; install hooks per checkout. See `memory/session_continuity_gap.md`.
- [x] **Lockfile LOC noise** — `.gitattributes` `linguist-generated` marker already existed (GitHub-UI-only, doesn't touch local metrics). Added `scripts/loc-filtered.ps1` as the standard local command: computes insertions/deletions/net with lockfiles excluded, reports lockfile churn on a separate line. `/retro` itself is an external gstack plugin, not editable from this repo — this gives a canonical command to point at instead.
- [x] **Commit-tag hygiene** (2026-07-29) — `test:` prefix for test-primary commits (was hiding inside `fix:`/`feat:`, undercounting test investment) and `chore(sync):` prefix for bulk/automated resync commits (a 506-file `.ijfw/` resync ate half a week's raw LOC total). Both documented in `CLAUDE.md` Productivity Discipline items 4-5.
- [x] **trm-vault commit-per-run** (2026-07-30) — standing rule: commit trm-vault after every real ingest/dedup run, not batched. Root-caused a 227-file uncommitted exposure window that also masked as a false "dark day" in this repo's retro (real work landed in trm-vault, invisible to `c:\dev` git log). See `memory/feedback_trm_vault_commit_per_run.md`.

## Log

- 2026-07-15: File created. Backlog had been memory-only since 07-12; flagged twice in retro (07-12, 07-15) as a recurring gap. Decision: create real file instead of deferring again.
- 2026-07-17: Dedicated backlog pass (file hadn't shrunk in 2 retros). Closed 1 item: "uncommitted files at repo root" — confirmed committed in `3e39dd0`, no longer stale. 4 items remain open, all genuinely blocked (infra, low-priority deferral, or unresolved tooling gap) — not neglect, no false closures forced.
- 2026-07-17 (later): Skill triage/migration + `run-tool.ps1` repair + security scanner fix session logged. 3 items closed, 3 new items opened (post-merge hook, dual-repo drift, run-tool.ps1 test coverage) — all real, found during the session, not deferred busywork.
- 2026-07-26: Closed session continuity and lockfile LOC items. Verified installed `commit-msg` hook, hook generator coverage, and `.gitattributes` lockfile attributes. Continuity check remains advisory rather than blocking.
- 2026-07-28: Benson Ford ingest closed (256/256 real files). Opened 3 items: TRM-to-treatment sync skill, TRM feedback/report skill, video ingestion plan — all net-new capability gaps surfaced by running a real large-batch ingest for the first time. See `memory/session-wrap-2026-07-28-benson-ford-close.md`.
- 2026-07-29: Retro-driven session. Closed CHANGELOG staleness watch (17-commit gap backfilled as v2.24.0). Opened commit-tag hygiene convention (`test:`, `chore(sync):`) in `CLAUDE.md`. Also fixed a real CRITICAL finding: `trm-feedback-report`'s `child_process` import needed a Tier 1 SEC-AUDITOR exemption (execFileSync, array args, hardcoded binary — no shell-injection surface) before push would pass.
- 2026-08-02: Retro-driven fixes. (1) Root-caused the self-regenerating pre-commit/auto-sync loop (2nd retro flagging it): `cowork-auto-sync.ps1` + 4 downstream generators (`toolforgeSkillValidator.ps1`, `toolforgeDependencyGraph.ps1`, `toolforgeSkillHealthCheck.ps1`, `toolforgeMetadataGenerator.ps1`) unconditionally `Set-Content`'d fresh ISO timestamps every run, and only 2 of 5 had loop guards — nested calls re-ran siblings redundantly. Added `Write-IfChanged` (skip write when content is identical modulo embedded timestamps) to all 8 write sites, added missing loop guards to the 3 unguarded generators, and fixed a real nondeterminism bug in `Phase-SyncSkills` (unsorted PowerShell hashtable `.Keys` enumeration was shuffling the sync-history log order every run, defeating any diff check). Verified via 4 consecutive live runs: steady-state output is now byte-identical modulo timestamps. (2) Added `[P0]/[P1]/[P2]` priority tags to all 8 open items — no P0s currently; 4×P1 (kb-sync coverage, kb-sync drift, Wave D gate, toolforge-install node_modules bug); 5×P2 (video ingestion plan, 2 TorqueQuery items, xberg, trm-vault retro-tooling residual). (3) `4f66345f` (12.7k-LOC research-artifact resync, predates the `chore(sync):` convention by 3 days) not retagged — retroactive retag doesn't fix already-computed metrics; convention already governs new commits going forward (`CLAUDE.md` Productivity Discipline #5).
- 2026-07-31: Synced 3 retro files (`2026-07-29-1`, `2026-07-29-2`, `2026-07-30-1.json`) flagged unaddressed by the new `check-todo-sync.js` Stop hook. `2026-07-29-1` was a same-window duplicate of `2026-07-29-2` (both already covered). `2026-07-30-1` surfaced 2 genuinely new items: opened `cic-ingestion tsx bypasses tsconfig type-checking` and `trm-vault has no git remote` (Open); closed/logged `trm-vault commit-per-run` as an adopted Process rule (was memory-only, not yet in this file).
- 2026-08-06: Retro follow-through on 3 improvement items. (1) `scripts/loc-filtered.ps1` now excludes `chore(sync):`-tagged commits from the headline code metric automatically (parses commit subject via a `--pretty` marker line ahead of each `--numstat` block, reports sync-commit churn on its own line) — 3rd time this exclusion needed manual correction in a retro before being scripted. (2) Split `## Open` from a new `## Completed` section — 40+ `[x]` items were interleaved with the 6 still-open ones, making the real backlog hard to see at a glance. (3) Added a creation-date-stamp convention (`**Priority tags**` note above) so future retros can compute backlog-added-this-period, not just net open count; applies going forward, no retroactive backfill. Did not action the TRM sync-treatment fixture-realism note (deployment-surface test coverage, e.g. scheduler exit codes / live vault dirs) — no active TRM work this session to attach it to; flagged here for next TRM touch.
- 2026-08-15: Retro follow-through. Consolidated 19 auto-generated kb-sync drift detector entries into a single batched [P2] backlog item; updated sibling checker to maintain a single consolidated drift entry instead of emitting per-file TODOs.





































