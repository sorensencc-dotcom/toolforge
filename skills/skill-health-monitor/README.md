# skill-health-monitor

Ecosystem health from real `manifest.json` data: staleness, never-run skills,
and manifest/directory drift. Migrated from `~/.claude/skills/skill-health-monitor.md`
(global stub — hardcoded fake metrics) on 2026-07-17; rewritten to only report data
that actually exists. See `SKILL.md` for full spec and how this differs from
`rewrite-docs/scripts/skill-health-monitor.ts`.

```
npm test
```
