# Parallel Search

Fail-closed TypeScript wrappers for Parallel Search, Extract, and asynchronous Task Run creation.`n`n## Purpose`n`nProvide deterministic current-web operations without exposing provider credentials or raw provider failures.`n`n## Usage`n`nSet `PARALLEL_API_KEY`, then call exports from `src/index.ts`.`n`n## Permissions`n`nRequires outbound web access to Parallel and no filesystem write access.

See [docs/USAGE.md](docs/USAGE.md) for inputs, outputs, and error handling.
