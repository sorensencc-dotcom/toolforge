# Guide to system setup

To configure the client, supply your API key in the environment.

## Database connection pooling

This update reduces memory overhead by 40%. The scheduler triggers the backup job nightly.

See the [PostgreSQL Connection Guide](docs/db.md).

Supports JSON, YAML, and TOML formats.

1. Clone the repository.
2. Install dependencies.
3. Run the test suite.
