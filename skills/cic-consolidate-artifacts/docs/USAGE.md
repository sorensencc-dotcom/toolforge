# cic-consolidate-artifacts

Phase 1 stub. Returns fabricated consolidation result; no real consolidation runs.
Paths are relative to repo root (`<repo-root>/cic/artifacts/...`) because this repo runs on Windows.

## Input
`{ runIds: string[]; profile?: string }`

## Output
`{ bundleId, status: "stub", bundlePath, timestamp }`
