# session-wrap

Atomic session wrap-up: doc updates → scoped git stage → prefixed commit → report.

Migrated from `~/.claude/skills/session-wrap.md` (global, v1.0.0) on 2026-07-17.
See `SKILL.md` for the v1.1.0 changes and full spec.

```
npm test
```
